# Copy to `.env` at the repository root. Values are read by the FastAPI app (AURALIA_*)
# and the Vite dev server (VITE_*).

# --- Backend (Pydantic Settings, prefix AURALIA_) ---
AURALIA_API_HOST=0.0.0.0
AURALIA_API_PORT=8000
AURALIA_SQLITE_PATH=data/db/auralia.sqlite
# Comma-separated browser origins allowed for CORS during local dev
# (TanStack Start CLI defaults the web app to port 3000; include both if you use either.)
AURALIA_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# --- Segmentation + Attribution (Ollama + Qwen) ---
AURALIA_OLLAMA_BASE_URL=http://localhost:11434
AURALIA_SEGMENTATION_MODEL=qwen3:8b
AURALIA_CHUNK_SIZE=3000
AURALIA_CHUNK_OVERLAP=200
AURALIA_SEGMENTATION_MAX_RETRIES=3
AURALIA_OLLAMA_TIMEOUT_SECONDS=120

AURALIA_ATTRIBUTION_MODEL=qwen3:8b
AURALIA_ATTRIBUTION_CONFIDENCE_THRESHOLD=0.7
AURALIA_ATTRIBUTION_MAX_WINDOW_DIALOGUES=12
AURALIA_ATTRIBUTION_MAX_WINDOW_CHARS=6000
AURALIA_ATTRIBUTION_MAX_GAP_CHARS=400
AURALIA_ATTRIBUTION_MAX_RETRIES=3

# --- Local Qwen3-TTS ---
# Model IDs may also be local directories after downloading weights.
AURALIA_QWEN_TTS_PYTHON=
AURALIA_QWEN_TTS_VOICE_DESIGN_MODEL=Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign
AURALIA_QWEN_TTS_VOICE_CLONE_MODEL=Qwen/Qwen3-TTS-12Hz-1.7B-Base
AURALIA_QWEN_TTS_DEVICE=cuda:0
AURALIA_QWEN_TTS_DTYPE=bfloat16
AURALIA_QWEN_TTS_DEFAULT_LANGUAGE=English
AURALIA_QWEN_TTS_TIMEOUT_SECONDS=300
AURALIA_QWEN_TTS_NUMBA_CACHE_DIR=/tmp/auralia-numba-cache

# --- Frontend (Vite: only VITE_* are exposed to the browser) ---
# Leave empty to use same-origin requests (recommended with Vite proxy in dev).
# Example for a remote API: VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=
